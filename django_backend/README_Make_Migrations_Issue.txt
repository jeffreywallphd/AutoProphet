after running make migrations errors pop up You may need to install the following packages
make migrations

pip install django
pip install python-decouple
pip install djangorestframework
pip install django-cors-headers
pip install mysqlclient 